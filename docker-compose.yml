version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on: []
    volumes:
      - ./server:/app
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - server
